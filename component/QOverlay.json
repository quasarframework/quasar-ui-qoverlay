{
  "type": "component",
  "props": {
    "value": {
      "type": "Boolean",
      "desc": "Use v-model to control active state"
    },
    "opacity": {
      "type": [ "Number", "String" ],
      "desc": "Used to change the opacity of the overlay",
      "default": "0.35",
      "examples": ["0.50"]
    },
    "z-index": {
      "type": [ "Number", "String" ],
      "desc": "Used to change the z-index of the overlay",
      "default": "5000",
      "examples": [ "2000" ]
    },
    "cursor-type": {
      "type": "String",
      "desc": "Used to change the cursor type of the overlay",
      "default": "not-allowed",
      "values": [ "not-allowed", "pointer", "none" ],
      "examples": [ "pointer" ]
    }
  },
  "slots": {
    "default": {
      "desc": "Anything can go into this slot"
    },
    "body": {
      "desc": "Anything using the body slot will be placed above the overlay"
    }
  }
}